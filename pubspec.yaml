name: muslim_daily
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  hijri: ^3.0.0
  table_calendar: ^3.0.0
  timezone: ^0.10.0
  flutter_timezone: ^4.1.1
  flutter_qiblah: ^3.1.0+1
  geolocator: ^13.0.4
  permission_handler: ^11.3.0
  sensors_plus: ^4.0.1
  url_launcher: ^6.2.6
  google_maps_flutter: ^2.5.0
  http: ^1.2.1
  shared_preferences: ^2.0.13
  intl: ^0.20.2
  adhan: ^2.0.0
  csv: ^5.0.0
  flutter_typeahead: ^5.2.0
  flutter_local_notifications: ^19.4.0
  awesome_notifications: ^0.10.1

  # Use local version of flutter_native_timezone with namespace fix

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/img.png
    - assets/worldcities.csv

